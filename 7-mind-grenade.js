const num1 = 5
const num2 = 10

function addValues(){
    console.log(`the sum is : ${num1 + num2}`)
}

addValues()

// if you use require(./7-mind-grenade.js) in the app.js, app.js will autoinvoke the addValues function